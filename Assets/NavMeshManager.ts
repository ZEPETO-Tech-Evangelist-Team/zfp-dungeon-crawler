import { Input, KeyCode } from 'UnityEngine'
import { NavMeshBuilder } from 'UnityEngine.AI'
import { ZepetoScriptBehaviour } from 'ZEPETO.Script'

export default class NavMeshManager extends ZepetoScriptBehaviour {

    Start() {    

    }

    Update() {
        if (Input.GetKeyDown(KeyCode.B)) {

        }
    }
}